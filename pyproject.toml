[project]
name = "rca-frontend"
version = "0.1.0"
description = "NiceGUI client for the AgentCore RCA agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nicegui>=2.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
]
