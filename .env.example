# Copy to `.env` and set values locally (never commit `.env`).
# Used by NiceGUI for signed session cookies backing `app.storage.user`.

# Long random string (e.g. `openssl rand -hex 32`). Required for user/session storage.
STORAGE_SECRET=
